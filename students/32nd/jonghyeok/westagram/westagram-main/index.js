//댓글 input에 글이 입력되고 게시버튼, enter 클릭 시 새로운 글이 입력이 된다.
//<div class="comment-box"> 에 글을 추가 한다.
//resultHtml = [] 입력값을 준다.
//resultHtml = [] 여기에 들어온 값을 <div class="comment-box"> 에 추가

const newComment = document.querySelector('.writing-input');
const addButton = document.querySelector('.writing-btn');
const tabs = document.querySelectorAll()

const addTask = []

addButton.addEventListener("click", addTask);


newComment.addEventListener('click', function(){
  const commentValue = newComment.value
  
})